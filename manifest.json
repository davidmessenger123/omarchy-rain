{
  "schemaVersion": 1,
  "id": "davidjm.rain",
  "name": "Rain",
  "version": "1.1.0",
  "author": "David JM",
  "license": "MIT",
  "description": "Background effects over the desktop wallpaper, behind your windows. Toggled from the bar: a GPU shader paints animated scenes on a full-screen background layer.",
  "kinds": ["bar-widget"],
  "entryPoints": {
    "barWidget": "Rain.qml"
  },
  "barWidget": {
    "displayName": "BackgroundFX",
    "description": "A wand icon that paints animated effects over the desktop wallpaper, behind every window. Pick an effect: rain with lightning, snowfall, puddle ripples, floating dust, fireflies, falling leaves (autumn or cherry blossom), an aurora, or light shafts from a chosen corner. Click to toggle; right-click for the effect switch, leaf-style and corner sub-menus, continuous intensity and speed sliders. Effects render on a background layer behind every window, so your apps stay on top.",
    "category": "Effects",
    "allowMultiple": true,
    "schema": [
      {
        "key": "effect",
        "type": "enum",
        "label": "Effect",
        "options": ["Rain", "Snow", "Ripples", "Dust", "Fireflies", "Leaves", "Aurora", "Embers", "Bubbles", "Confetti", "Caustics", "Light Shafts"],
        "defaultValue": "Rain",
        "description": "The background effect painted over the wallpaper."
      },
      {
        "key": "variant",
        "type": "enum",
        "label": "Leaf style",
        "options": ["autumn", "cherry"],
        "defaultValue": "autumn",
        "description": "Falling Leaves effect: the same tumbling leaves, recolored — autumn rust/gold, or cherry blossom white/pink."
      },
      {
        "key": "corner",
        "type": "enum",
        "label": "Light source corner",
        "options": ["tl", "tr", "bl", "br"],
        "defaultValue": "tl",
        "description": "Light Shafts effect: which corner the shafts stream from — top-left, top-right, bottom-left, or bottom-right."
      },
      {
        "key": "straightness",
        "type": "integer",
        "label": "Straightness",
        "min": 0,
        "max": 2,
        "step": 1,
        "defaultValue": 1,
        "description": "Light Shafts effect: how straight the beams are — 0 = wavering and curving, 1 = subtly bowed (default), 2 = ruler-straight. Non-integer in practice (the slider writes e.g. 1.3)."
      },
      {
        "key": "density",
        "type": "integer",
        "label": "Intensity",
        "min": 1,
        "max": 3,
        "step": 1,
        "defaultValue": 2,
        "description": "Continuous intensity, 1 = light to 3 = heavy. Meaning per effect: rain intensity, snow density, leaf count, firefly amount, aurora brightness, dust amount. Non-integer in practice (the slider writes e.g. 2.4); the schema type is limited to integer for the editor."
      },
      {
        "key": "speed",
        "type": "integer",
        "label": "Speed",
        "min": 1,
        "max": 3,
        "step": 1,
        "defaultValue": 1,
        "description": "Continuous speed controlling the effect's motion. Non-integer in practice (the slider writes e.g. 1.02)."
      },
      {
        "key": "fps",
        "type": "integer",
        "label": "Framerate",
        "min": 15,
        "max": 60,
        "step": 1,
        "defaultValue": 60,
        "description": "Frames per second the effect animates at. Lower = less GPU work, choppier motion. Applies to every effect."
      },
      {
        "key": "quality",
        "type": "enum",
        "label": "Resolution",
        "options": ["0.5", "1", "1.5", "2"],
        "defaultValue": "1",
        "description": "Render scale relative to native resolution: 0.5x = a quarter of the pixels (softer, far cheaper), 1x = native, 2x = supersampled. Increments of 0.5x. Applies to every effect."
      },
      {
        "key": "lightning",
        "type": "boolean",
        "label": "Lightning flashes",
        "defaultValue": true,
        "description": "Rain effect: random real lightning bolts (with a soft sky flash) drawn by the shader."
      },
      {
        "key": "audio",
        "type": "boolean",
        "label": "Audio reactive (Aurora)",
        "defaultValue": false,
        "description": "Aurora effect: the curtain reacts to system audio, swells with the music and shimmers faster."
      }
    ]
  }
}